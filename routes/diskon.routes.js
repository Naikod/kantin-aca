const express = require('express');
const app = express();
const diskonController = require('../controllers/diskon.controller');
// const auth = require('../auth/auth');

// app.post('/adddiscount', diskonController.addDiskon); 
// app.put('/:id', diskonController.updateDiskon); 
// app.get("/:id", diskonController.getDiskon);      
// app.get("/", diskonController.getAllDiskon);      
// app.get("/:search", diskonController.getSiswa);

module.exports = app;